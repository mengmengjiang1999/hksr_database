## 1. Read-only browser API

- [x] 1.1 Add deterministic catalog facets and source processing counts to the database read layer
- [x] 1.2 Expose the read-only `GET /api/catalog` contract and verify source traceability fields
- [x] 1.3 Add API tests proving catalog reads have no side effects and filters remain compatible

## 2. Knowledge workspace UI

- [x] 2.1 Build hash-based navigation for ask, search, coverage, and source-detail views
- [x] 2.2 Build filterable evidence search with result metadata and source-detail links
- [x] 2.3 Build traceable source details and a collection-aware coverage dashboard
- [x] 2.4 Preserve claim-level citations and relations while adding explicit loading, empty, insufficient-evidence, and error states
- [x] 2.5 Complete responsive styling, keyboard focus behavior, and live status semantics

## 3. Verification and handoff

- [x] 3.1 Extend static UI contract tests and run API/UI regression tests
- [x] 3.2 Run the full local test suite and strict OpenSpec validation
- [x] 3.3 Document the future-version incremental refresh workflow and remaining M7 dependency
