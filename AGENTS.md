# 项目协作规则

## Bug 修复后的提交与推送规则

对于每个独立 Bug，当且仅当满足以下条件时，创建一次独立提交并推送：

1. Bug 已经修复；
2. 对应测试、复现步骤或其他验证已经通过；
3. 已检查 Git 差异，提交中不包含无关改动、凭据、运行数据或生成文件。

执行要求：

- 每个独立 Bug 使用一个单独 commit；
- 只暂存与该 Bug 有关的文件，不使用 `git add -A`；
- commit message 使用 `fix: 简要说明` 或 `fix(scope): 简要说明`；
- commit 成功后执行普通 `git push`，将当前分支推送到已配置的上游；
- 推送后检查 Git 状态并报告 commit hash；
- 如果测试失败，不得提交或推送；
- 如果推送被拒绝、远端领先或发生冲突，停止并报告，不自动 pull、merge、rebase 或覆盖远端；
- 永远不得使用 `git push --force` 或 `git push --force-with-lease`；
- 用户明确要求“不提交”或“不推送”时，以该次要求为准。

用户通过本规则明确授权：对已经验证通过的 Bug 修复执行普通 commit 和普通 push。此授权不包含强制推送、历史改写或提交无关改动。

## Bug 修复庆祝规则

当且仅当满足以下所有条件时触发庆祝：

1. 当前任务确实属于 Bug 修复；
2. 修复已经完成；
3. 相关测试、复现步骤或其他验证已经通过。

满足条件后，调用 Codex 的礼花效果一次，并使用以下自定义 Emoji，保持顺序不变：

💩 🎉 🎊 ✨ 🚀

每个 Bug 修复任务最多播放一次。不要仅因为修改了代码或任务暂时结束就播放；验证失败时不要播放。
